name: flute
description: Flutter best friend for coding!
version: 1.0.0
homepage: https://github.com/ragokan/flute
repository: https://github.com/ragokan/flute

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
